name: Build Vim

on: push

env:
  VIMREPO: https://hg.osdn.net/view/vim/vim
  MQREPO: http://hg.pf.osdn.net/view/k/k_/k_takata/vim-ktakata-mq
  HG: hg --config extensions.mq= --config "ui.username=%USERNAME% <%USERNAME%@%USERDOMAIN%>"


jobs:
  build:
    runs-on: windows-latest

    strategy:
      matrix:
        arch: [x64, x86]

    steps:
    - uses: actions/checkout@v2

    - name: Checkout Vim
      shell: cmd
      run: |
        call %HG% qclone %VIMREPO% -p %MQREPO% vim
        cd vim
        hg log -v -r -1:-4
        call %HG% qpush -a
        cd ..

    - name: Build
      shell: cmd
      run: |
        if "${{ matrix.arch }}"=="x64" (
          set VCARCH=amd64
        ) else (
          set VCARCH=x86
        )
        call "C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\VC\Auxiliary\Build\vcvarsall.bat" %VCARCH%
        cd vim\src
        nmake -f Make_mvc.mak GUI=yes IME=yes ICONV=yes VIMDLL=yes
        rem Show gvim version
        start /wait .\gvim -u NONE -c "redir @a | ver | 0put a | wq!" ver.txt
        type ver.txt
        rem Show vim version
        .\vim --version

# vim: ts=2 sw=2 sts=2 et
